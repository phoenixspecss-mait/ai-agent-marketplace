services:
  - type: web
    name: ai-marketplace-agents
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn agent_routes:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
